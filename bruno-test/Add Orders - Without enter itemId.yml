info:
  name: Add Orders - Without enter itemId
  type: http
  seq: 13

http:
  method: POST
  url: "{{baseUrl}}/orders"
  body:
    type: json
    data: |-
      {
        "quantity": {{itemQuantity}}
      }
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  variables:
    - name: itemQuantity
      value: "1"
  assertions:
    - expression: res.status
      operator: eq
      value: "400"
    - expression: res.body.status
      operator: neq
      value: created
    - expression: res.body.error
      operator: isNotEmpty
    - expression: res.body.error
      operator: neq
      value: Item not found

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
