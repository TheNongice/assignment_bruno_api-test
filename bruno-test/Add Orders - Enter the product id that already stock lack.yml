info:
  name: Add Orders - Enter the product id that already stock lack
  type: http
  seq: 11

http:
  method: POST
  url: "{{baseUrl}}/orders"
  body:
    type: json
    data: |-
      {
        "itemId": {{itemId}},
        "quantity": {{itemQuantity}}
      }
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  variables:
    - name: itemId
      value: "1"
    - name: itemQuantity
      value: "1"
  assertions:
    - expression: res.status
      operator: eq
      value: "400"
    - expression: res.body.status
      operator: neq
      value: created
    - expression: res.body.error
      operator: isNotEmpty

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
