info:
  name: Get Orders
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/orders/ORD-379491"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
